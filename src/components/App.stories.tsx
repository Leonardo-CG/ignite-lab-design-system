import { Meta, StoryObj } from '@storybook/react'
import { App, AppProps } from '../App'

export default{
    title: 'Components/App',
    component: App,
    args: {},
    argTypes: {}
} as Meta<AppProps>

export const Default: StoryObj<AppProps> = {}